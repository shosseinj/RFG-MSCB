\section{Ablation Study}\label{ablation-study}

The ablation design compares three configurations in sequence: Baseline,
FAFEM + MSCB, and Residual RFG-MSCB (Ours). This sequence evaluates the
feature-enhancement and multi-scale components together before assessing the
additional residual frequency-guided modification.

\subsection{Ablation Results}

The Baseline provides the reference without the added FAFEM and MSCB components.

Adding FAFEM and MSCB improves the baseline across the reported segmentation
metrics. The final configuration then adds residual frequency-guided coupling to
the multi-scale configuration. Table~\ref{tab:ablation} reports the simple arithmetic
mean of each segmentation metric across Kvasir-SEG, CVC-ClinicDB, CVC-300,
CVC-ColonDB, and ETIS-LaribPolypDB, together with model complexity.

A single-seed direct frequency-guidance configuration tested the mechanism without
residual coupling. Its Kvasir-SEG result was lower than that of FAFEM + MSCB in
the paired seed-42 comparison.
% TODO: replace the direct-guidance comparison after rerunning it under the final data protocol.

\begin{table*}[htbp]
\centering
\caption{Ablation comparison averaged across the five polyp segmentation datasets, with computational complexity.}\label{tab:ablation}
\small
\setlength{\tabcolsep}{3pt}
\renewcommand{\arraystretch}{1.1}
\begin{tabular}{@{}lrrrrrrrrr@{}}
\toprule
Model & mDice & mIoU & $F_{\beta}^{w}$ & $S_{\alpha}$ & $mE_{\phi}$ & $\max E_{\phi}$ & MAE & Params (M) & GFLOPs \\
\midrule
Baseline & 0.827 & 0.763 & 0.811 & 0.890 & 0.907 & 0.913 & 0.019 & 29.19 & 27.202 \\
FAFEM + MSCB & \underline{0.869} & \underline{0.803} & \underline{0.845} & \underline{0.910} & \underline{0.937} & \underline{0.947} & \underline{0.018} & 29.98 & 27.818 \\
\textbf{Residual RFG-MSCB (Ours)} & \textbf{0.887} & \textbf{0.832} & \textbf{0.864} & \textbf{0.923} & \textbf{0.952} & \textbf{0.956} & \textbf{0.014} & 30.06 & 27.818 \\
\bottomrule
\end{tabular}
\end{table*}

\subsection{Ablation Interpretation}

Across the five-dataset averages, FAFEM + MSCB increases mDice, mIoU,
$F_{\beta}^{w}$, $S_{\alpha}$, $mE_{\phi}$, and $\max E_{\phi}$ over the Baseline
by 0.042, 0.040, 0.034, 0.020, 0.030, and 0.034, respectively, corresponding to
gains of 4.2, 4.0, 3.4, 2.0, 3.0, and 3.4 percentage points. It reduces MAE by
0.001. Residual RFG-MSCB further increases these six metrics over FAFEM + MSCB
by 0.018, 0.029, 0.019, 0.013, 0.015, and 0.009, respectively (1.8, 2.9, 1.9,
1.3, 1.5, and 0.9 percentage points), and reduces MAE by 0.004. The parameter
count increases from 29.19 M to 29.98 M and then 30.06 M, changes of 0.79 M and
0.08 M, while GFLOPs increase from 27.202 to 27.818 and then remain unchanged,
changes of 0.616 and 0.000. These are descriptive comparisons; no statistical
significance is claimed.

\subsection{Stage Placement and Generalization}

% TODO: update placement and cross-dataset observations after the final reruns.
Stage 3 is used in the selected configuration. The exploratory all-skip
experiment used a single seed, was incomplete, and used batch size 32 with the same
learning rate. Its training result was lower than that of the Stage-3 configuration.
A controlled multi-seed placement study remains future work.

On the four additional datasets, the change from FAFEM + MSCB to the final guidance
configuration was mixed in the paired seed-42 comparison: mDice decreased on
CVC-ClinicDB and CVC-300 and increased on CVC-ColonDB and ETIS. The residual-coupling configuration
with default initialization did not complete training and is therefore excluded from
the quantitative ablation; initialization sensitivity remains a subject for future
work.
